import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agropec.leite',
  appName: 'agropecfuturoleite-web',
  webDir: 'dist'
};

export default config;
